import { Propriedades } from './base.model';

describe('dominio:entidades:componentes:base', () => {
    it('deve criar uma instância de [Propriedades]', () => {
        expect(new Propriedades()).toBeTruthy();
    });
});
